import React from 'react';

import {Grid} from '@mui/material';

const CheckEmail = () => {
    return (
        <Grid container>
            <h3>Check your email, we sent you a form for a password change.</h3>
        </Grid>
    );
};

export default CheckEmail;
