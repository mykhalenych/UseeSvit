export const AUTH_PATH = '/auth';
export const LOGIN_PATH = `${AUTH_PATH}/login`;
export const FORGOT_PATH = `${AUTH_PATH}/forgot-password`;
export const SIGN_UP_PATH = `${AUTH_PATH}/sign-up`;
export const ACTIVATION_PATH = `${AUTH_PATH}/activation/:activationToken`;
export const CHECK_EMAIL_PATH = `${AUTH_PATH}/check-email`;
export const RESET_PASSWORD_PATH = `${AUTH_PATH}/reset-password/:resetToken`;

export const PLAN_PATH = '/plan';

export const PROFILE_PATH = '/profile';
