import {styled} from '@mui/material/styles';

export const BoxLoader = styled('div')(() => ({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    textAlign: 'center',
    zIndex: '500',
}));
