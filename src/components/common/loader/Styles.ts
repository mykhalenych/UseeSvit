import {styled} from '@mui/material/styles';

export const BoxLoader = styled('div')(() => ({
    width: '100vw',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    textAlign: 'center',
    zIndex: '500',
}));
