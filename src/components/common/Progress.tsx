import React from 'react';
import {Stack, CircularProgress} from '@mui/material';

export const Progress = () => {
    return (
        <Stack spacing={2}>
            <CircularProgress />
        </Stack>
    );
};
